import React from 'react';
import MainTemplate from '../templates/MainTemplate';
import Form from '../organisms/Form';

const FormPage = () => (
  <MainTemplate>
    <h1>And let's not forget about the form page, where you can add you data and then start your trainings</h1>
    <Form />
  </MainTemplate>
);

export default FormPage;